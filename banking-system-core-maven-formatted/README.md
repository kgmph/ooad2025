# Banking System Core (Maven) — formatted output

## Run (compile + run)
```bash
mvn -q exec:java
```

## Package JAR
```bash
mvn -q -DskipTests package
```
Outputs: `target/banking-system-core-1.0.1.jar`
